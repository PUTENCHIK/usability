import Felgo
import QtQuick
import QtQuick.Window 2.15

Rectangle {
    color: "#dcdcdc"

    height: 100

    Text {
        id:header_text
        text: "Header"
        anchors.centerIn: parent
        font.pixelSize: 20
    }
}
